import { CssStyleDirective } from './css-style.directive';

describe('CssStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CssStyleDirective();
    expect(directive).toBeTruthy();
  });
});
