import { CsshighlightDirective } from './csshighlight.directive';

describe('CsshighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CsshighlightDirective();
    expect(directive).toBeTruthy();
  });
});
