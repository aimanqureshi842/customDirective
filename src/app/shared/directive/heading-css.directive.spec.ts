import { HeadingCssDirective } from './heading-css.directive';

describe('HeadingCssDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingCssDirective();
    expect(directive).toBeTruthy();
  });
});
